export const MS=1n, SEC=1000n*MS, MIN=60n*SEC, HOUR=60n*MIN, DAY=24n*HOUR
export const YEAR=365n*DAY, MONTH=30n*DAY
export const SOFT_YEARS=1000n
export function bigAddStr(a,b){ return (BigInt(a||'0')+BigInt(b||'0')).toString() }
export function briefHuman(ms){ let x=BigInt(ms); const years=x/YEAR; if(years>=1_000_000n) return `≈ ${(Number(years)/1_000_000).toFixed(1)} млн лет`; if(years>=1_000n) return `≈ ${(Number(years)/1_000).toFixed(1)} тыс. лет`; if(years>=100n) return `≈ ${Number(years)} лет`; if(years>=1n){const months=(x%YEAR)/MONTH; if(months>0n) return `${Number(years)}г ${Number(months)}мес`; return `${Number(years)}г`;} const months=x/MONTH; if(months>=1n){const days=(x%MONTH)/DAY; if(days>0n) return `${Number(months)}мес ${Number(days)}д`; return `${Number(months)}мес`;} const days=x/DAY; if(days>=1n){const hours=(x%DAY)/HOUR; if(hours>0n) return `${Number(days)}д ${Number(hours)}ч`; return `${Number(days)}д`;} const hours=x/HOUR; if(hours>=1n){const mins=(x%HOUR)/MIN; if(mins>0n) return `${Number(hours)}м ${Number(mins)}м`; return `${Number(hours)}ч`;} const mins=x/MIN; if(mins>=1n){const sec=x%MIN; return `${Number(mins)}м`;} const sec=x/SEC; return `${Number(sec)}с` }
export function hhmmss(ms){ let x=BigInt(ms); const h=x/HOUR; x%=HOUR; const m=x/MIN; x%=MIN; const s=x/SEC; const hh=String(Number(h)).padStart(2,'0'), mm=String(Number(m)).padStart(2,'0'), ss=String(Number(s)).padStart(2,'0'); return `${hh}:${mm}:${ss}` }
