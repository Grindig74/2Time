import React from 'react';function timeLeft(target){const now=new Date();const end=new Date(target);let diff=Math.max(0,end-now);const d=Math.floor(diff/86400000);diff-=d*86400000;const h=Math.floor(diff/3600000);diff-=h*3600000;const m=Math.floor(diff/60000);diff-=m*60000;const s=Math.floor(diff/1000);return{d,h,m,s}}export default function TimerCard({t,draggableProps}){const x=timeLeft(t.endsAt);return(<div className='text-3xl font-mono mt-1' draggable {...draggableProps}>{x.d}д {String(x.h).padStart(2,'0')}:{String(x.m).padStart(2,'0')}:{String(x.s).padStart(2,'0')}</div>)}